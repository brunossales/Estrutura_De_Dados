#include <iostream>
#include <stack>
using namespace std;

int main()
{
  int n, value;
  stack<int> inteiros;
  stack<int> indexs;
  vector<int> vector;
  cin >> n;
  for (int i = 0; i < n; i++)
  {
    cin >> vector[i];
    if (i == 0)
      indexs.push(i);
    else
    {
    }
  }
}